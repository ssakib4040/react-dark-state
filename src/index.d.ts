// Define the return type of the useDarkState hook
declare type DarkState = "dark" | "light" | null;

// Declare the useDarkState custom hook
declare function useDarkState(): DarkState;

// Export the custom hook type
export default useDarkState;
